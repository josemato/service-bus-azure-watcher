{
  "name": "service-bus-azure-watcher",
  "engines": {
    "node": ">=6.11.0"
  },
  "version": "1.0.3",
  "description": "Read azure bus service queue messages to be processed by an user",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jose Mato",
  "license": "ISC",
  "dependencies": {
    "promise-retry": "^1.1.1"
  }
}
